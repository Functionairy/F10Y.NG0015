# F10Y.NG0015
Microsoft.EntityFrameworkCore NuGet package selector.

This selector library is related to the F10Y.NG0014 (Microsoft.EntityFrameworkCore.SqlServer) in that libraries can take a dependency on only Microsoft.EntityFrameworkCore instead of also needing MS SQL.

Both the package versions for both selector libraries should be updated together.